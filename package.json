{
  "name": "metamask-image",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "docker run -it --rm metamask yarn browser",
    "build": "docker build -t metamask .",
    "browser": "ts-node src/browser/index.ts",
    "inspect": "docker run -it --rm metamask zsh",
    "vlm": "vlm shell -c /mnt/host metamask",
    "download-metamask": "ts-node scripts/fetchMetamask.ts"
  },
  "dependencies": {
    "@vlegm/utils": "^1.0.1",
    "adm-zip": "^0.5.9",
    "chalk": "^4.x",
    "puppeteer": "^13.5.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3",
    "xvfb": "^0.4.0"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.0",
    "@types/chalk": "^2.2.0"
  }
}
